# QA release v1.5.2 — 2026-08-26

Objetivo: alinear la elegibilidad de repaso con `targetRetention` vigente sin reescribir memoria.

Controles requeridos:
- sintaxis JS;
- QA estático;
- test `session-core`;
- smoke navegador;
- checksums;
- verificación de que `session-core.js` y `session-storage.js` siguen protegidos.
