# Aplicación v1.5.2

1. Reemplazar el repositorio/runtime por v1.5.2.
2. No ejecutar SQL nuevo. La migración v1.5.0 solo sigue siendo requisito si nunca fue aplicada.
3. Confirmar en pantalla `v1.5.2`.
4. Abrir el plan: debe conservar `Rescate ALTA + memoria`.
5. Iniciar `Repasos rentables` y comprobar continuidad normal.

No resetear `question_memory_state`, attempts ni sesiones.
